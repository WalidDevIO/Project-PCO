// Création d’un satellite à la hauteur de 2000 mètres
sat1 := new Satellite(hauteur = 2000) ;
// sat1 démarre
sat1.start() ;

// Création d’une balise
b1 := new Balise(deplacement = #horizontal, profondeur = 200) ;
// b1 démarre
b1.start() ;

sim := new Simulation2(config = #defaultConfig);

sim.addSatellite(sat1);
