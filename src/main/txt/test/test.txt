

sim := new Simulation() ;


sim.generateSatellites(count = 5) ;
sim.generateBalises(count = 10) ;

sim.startSimulation() ;




config := new DefaultConfig() ;
point := new Point(x=10,y=10) ;
deplacement := new DeplacementHorizontal(speed=5, config=config);

//Création d’un satellite à la hauteur de 2000 mètres
sat1 := new Satellite(deplacement=deplacement, point=point, config=config) ;
// sat1 démarre
//sat1.start() ;

// Création d’une balise
//b1 := new Balise(12,12) ;
// b1 démarre
//b1.start() ;
